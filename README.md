# Spur: example express project with tests


## Tests

To run the test suite, first install the dependancies, then run `mocha` or `mocha --watch`:

```bash
$ npm install -g nodemon
$ npm install
$ mocha
```

## Start server

```bash
$ nodemon src/start.js
```
